# 动态指导助手

当前版本：**v2.91**（发布标签：`v2.91`）

把完整剧情大纲写在角色世界书的条目里，用分段划成一幕一幕。动态指导助手会关闭来源条目，并在同一本世界书里维护一个「（动态指导）」镜像条目：位置、顺序、关键词等设置全部跟随原条目，内容只有当前这一幕。可以同时绑定好几条。没选依附的自己走。选了依附的，从某一段开始挂上去：分岔口是选了这条、原来那条就断；支线是可以走，走完回到原来那条接着往下。分岔口上还能再依附别的条目。

“编辑”仍是原来的分段：正文连续铺开，标题条内联，拖选文字改归属，↑↓ 换顺序。想直接改字就切到“编辑原文”。依附和循环在小卡的「设置 ›」里。

## 安装与开始使用

在 [v2.91 发布页](https://github.com/PlayerAlex/sillytavern-dynamic-guide/releases/tag/v2.91)下载导入文件：

- [离线版 JSON](https://github.com/PlayerAlex/sillytavern-dynamic-guide/releases/download/v2.91/dynamic-guide-offline-v2.91.json)：内置完整脚本，导入后运行不依赖远程脚本下载。
- [在线版 JSON](https://github.com/PlayerAlex/sillytavern-dynamic-guide/releases/download/v2.91/dynamic-guide-online-v2.91.json)：启动时加载固定版本的远程脚本。
- [标记隐藏 Regex](https://github.com/PlayerAlex/sillytavern-dynamic-guide/releases/download/v2.91/dynamic-guide-regex-marker-hide-v2.91.json)：可选配套——让 AI 回复末尾的完成标记完全不显示（只影响显示层，脚本仍会把它从存储里擦掉）。

1. 在酒馆助手中导入并启用“动态指导助手”脚本，或导入发布仓库的在线版 JSON。
2. 给角色绑定一个世界书，在其中新建“大纲”条目。可以直接使用下面的文本模板，也可以先写普通大纲，用空行分开段落。
3. 点击酒馆左下角魔法棒，打开“动态指导助手”。
4. 在「动态指导」页的「绑定世界书」卡里选择一个世界书，再选一个条目。这一行按状态变形：还没分阶段时按钮是「编辑」，分好了就变成「绑定」，已绑定则置灰显示「已绑定」。
5. 点「编辑」进分段。拖选文字后从底部选归属；点标题条改名称、类型和进入条件。想改整篇原文就切到「编辑原文」。
6. 点「保存」。脚本会保存正文、读回校验；条目已绑定时还会立刻按新正文同步镜像，进度按阶段名自动对上。
7. 回到「动态指导」页点「绑定」，条目立刻变成一张常驻小卡（条目名 + 世界书名 + 段数步进器：上一段 / 当前段 + 阶段名 + 进度条 / 下一段）。小卡下面是「编辑 ›」「设置 ›」。设置里可以选依附哪一条、从第几段开始、这是分岔口还是支线，也可以开循环。编辑是原来的分段，点进去会落在当前这一段上。小卡右上角的 × 会把条目重新打开并删掉它在当前聊天的进度（会先弹确认）。
8. 保存或导出带有该世界书的角色卡。阶段标题和规则就在条目正文中，会随条目一起分享；接收者启用脚本并添加这个条目即可使用。

“保存”只更新条目正文，进度按阶段名自动对上。修改阶段时若要保留当前聊天进度，用「保存」即可；要重置进度就在小卡上用 ← / → 手动拨回第一段。

## 在线加载

远程 `index.js` 会自行在左下角魔法棒菜单注册“动态指导助手”入口。酒馆助手脚本正文可以只写一行：

```js
import 'https://cdn.jsdelivr.net/gh/PlayerAlex/sillytavern-dynamic-guide@v2.91/index.js';
```

推荐直接导入[发布仓库](https://github.com/PlayerAlex/sillytavern-dynamic-guide)提供的在线版 JSON。固定标签地址不会随新版本发布而改变；升级时导入新版在线版 JSON，或替换地址中的版本号。当前项目版本、Git 标签及在线加载地址均使用 `v2.91`。

## v2.0 文本格式

标题单独占一行，标题下面的内容属于这个块，直到下一个标题为止。没有类型标记的标题就是剧情阶段，按正文中的顺序推进。

```text
## 雨夜初遇
完成：两人完成第一次正式交谈。
{{char}}第一次在雨夜遇见{{user}}。
暂时不要透露地下室的秘密。

## 地下室的声音
完成：{{user}}决定调查地下室。
夜深后，地下室会传来断续的敲击声。
{{char}}知道声音不正常，但仍不愿说明真相。

## 蚀月戒指 [附加]
从：地下室的声音
到：进入地下室
戒指能看见灵体。
副作用是佩戴者会逐渐分不清死人和活人。

## 进入地下室
完成：地下室事件结束。
两人正式进入地下室，现在可以逐步揭示地下室的秘密。

## 写作风格 [常驻]
保持阴冷克制的笔调，不代替{{user}}作决定。

## 作者备忘 [备注]
这一部分只供作者整理思路，不发送给 AI。
```

| 写法 | 作用 |
| --- | --- |
| `## 阶段名` | 一个剧情阶段，占一个主线进度位置。 |
| `完成：条件` | 由 AI 判断本次回复是否已满足条件，满足后自动推进。留空时按全局「自动推进」设置处理（默认手动点“下一段”）。 |
| `完成：自动` | 不预设具体条件，由 AI 自己判断这一阶段何时算充分展开。只影响这一个阶段，跨档位生效。 |
| `分支：组名` | 把这个阶段标记为某个分支组的候选。同一组名的阶段互斥：进入其中一个后，其余分支这次聊天就不再走。 |
| `## 名称 [附加]` | 物品、地点规则、秘密等附加内容，不占主线进度。 |
| `从：阶段名` / `到：阶段名` | 附加内容的有效范围，开始和结束阶段都包含在内。 |
| `## 名称 [常驻]` | 在每个尚在进行的阶段中一起发送。全部阶段完成后也停止发送。 |
| `## 名称 [备注]` | 只保存在正文中，不发送给 AI。 |
| `合并到：阶段名` | 把这一块的内容并进指定阶段，和它一起发送，但自己不算一个新的进度位置。 |

`合并到：` 用来处理一个阶段里内容不连续的情况。比如“进入地下室”这个阶段中间插了一段别的内容，后面还有它自己的第二段文字，就给后面那段写一个标题，并在标题下一行写 `合并到：进入地下室`。两段文字会一起发给 AI，进度仍然只算“进入地下室”这一段。

```text
## 进入地下室
两人正式进入地下室。

## 地下室的岔路
合并到：进入地下室
岔路口的线索只在这一段出现。
```

`分支：组名` 用来写互斥走向。给两个或几个连续的阶段写上同一个组名，它们就是一组分支：走到这里时按剧情进入其中一个，进入之后其余分支这次聊天就不再走、内容也不再发送。分支选择记在当前聊天里，新聊天从第一段开始时不带旧选择；开「循环」时绕回第一段会清空分支、全部重来。

```text
## 雨夜初遇
完成：两人完成第一次正式交谈。
{{char}}第一次在雨夜遇见{{user}}。

## 留下避雨
分支：去向
两人决定留在屋檐下等雨停。

## 冒雨分别
分支：去向
{{user}}转身走进雨里，两人就此分别。

## 再联系
完成：两人再次见面。
无论避雨还是分别，之后都可能再联系。
```

上面这个例子里，「留下避雨」和「冒雨分别」互斥：进了「留下避雨」，「冒雨分别」就会被跳过，直接走向「再联系」。阶段属性弹层里也有「分支组」输入框，和在原文里写 `分支：组名` 是同一份设置。

完成条件最好写成明确发生的事件，例如“双方已经交换真实姓名”。脚本不调用额外的 AI API：它把条件交给当前回复模型，要求模型满足条件后附加隐藏完成标记，再据此推进。判断有偏差时可手动修正。

附加范围建议直接填写唯一的阶段名，也支持 `第2段`、`2` 等阶段序号。省略“从”时默认从它前面的剧情阶段开始；放在第一阶段前的附加内容默认归第一阶段。省略“到”时只在开始阶段有效。找不到开始阶段时回退到所在阶段，找不到结束阶段时回退到开始阶段；结束早于开始时只在开始阶段有效。管理页和编辑器会对这些情况给出提醒。

格式细节：

- 推荐始终使用 `## 名称`。解析器也接受 1～6 级 Markdown 标题和 `【名称】`；这些都是同级内容块，不表示嵌套。正文中的普通 Markdown 小标题也会开始一个新块。
- 第一个标题之前的文字不发送给 AI。每个阶段中可以有多个空行分隔的段落，它们仍属于同一阶段。
- 标题后的 `[附加]`、`[常驻]`、`[备注]` 决定内容类型；类型标记应放在整行末尾。
- `完成条件：` 与 `完成：` 等价。标签和值写在同一行最清楚；标签单独一行时，后续连续非空行会作为它的值，直到空行、另一标签或标题。
- 阶段名称建议保持唯一，便于附加范围引用和编辑后的进度定位。正文中的 `完成：`、`从：`、`到：` 等格式会被当作规则标签，不当作普通指导正文。
- 若要把标题或规则标签当作普通指导文字保留，在行首加一个反斜杠，例如 `\## 普通小标题`、`\【人物状态】`、`\完成：这句话只是正文`。这些行不会创建阶段或设置规则，发送给 AI 时会去掉转义用的反斜杠。

## 划分阶段编辑器

“划分阶段”里有三个视图，编辑的都是世界书条目本身的内容，随时可以来回切换：

- **看分段**：按空行把正文展示成可点击的段落，用来点选设置标题。
- **选区划分**：把正文铺成一段连续文字，用拖选或点选把文字分给各个分段。
- **编辑原文**：直接编辑条目正文的文本框，增删文字、调整顺序、自己写 `## 阶段名` 都行。切回“看分段”会重新按标题解析一次。

“选区划分”里的操作：

- **选分段**：顶部每个分段一枚彩色圆点 chip（阶段、附加、常驻提示、备注），点一下设为当前分段；`+ 阶段` / `+ 附加` 新建。
- **拖选**：在正文上按住拖出一段文字，松手后进入“已准备 N 段”；可以接着拖选更多段，再点“分配给「当前分段」”一次分完。触屏不好拖选时点“点选头尾”：点一下正文设开头，再点一下设结尾。
- **分配即重建**：分配、移除、删除分段会立刻按归属重写正文标题——阶段的顺序跟着它第一段文字的位置走，没分配的文字留在最前面（第一个标题之前，不会发给 AI，下次打开还能继续分配）。
- **点已上色的文字**：把它所在的分段设为当前分段并选中那一段；选区栏出现“移除选中段”，点了这段文字就回到未分配。
- **改名和条件**：在 chip 下方的设置区改名称、完成条件、附加的起止阶段。这类修改不触发重建，切视图或保存时才一次性写回，打字不会丢焦点。

“看分段”里的操作：

- **点段落**：在这一段前添加标题，选择剧情阶段、附加内容、常驻或备注。已有阶段中的段落也可以成为新阶段的开头。
- **把第一行变成标题**：当段落首行较短、看起来像标题时，可以勾选“这一段的第一行就是标题，把它变成标题行”；取消勾选则在原段落前插入新标题。
- **点已有标题**：修改名称、类型、完成条件；附加内容可以从下拉列表选择起止阶段。剧情阶段改名时，“从 / 到”中可解析到旧阶段名的引用会同步修改，包括带书名号或引号的旧写法；阶段序号引用保持不变。
- **上移 / 下移**：标题卡右侧的 ↑↓ 把这一块（标题、标签、正文）整体挪动一个位置。阶段顺序就是进度顺序；常驻块挪到所有阶段之前时，注入里排在当前阶段内容**之前**，写在阶段之间或最后则排在附加内容区——“常驻提示在上面还是下面”由它的位置决定。选区划分的重建会保留这个位置，常驻 chip 的设置区也有“排在阶段内容之前 / 之后”的开关。
- **删除标题**：移除该标题及其规则标签，正文并入前一个内容块。删除第一个标题后，留在第一个标题之前的文字不发送。
- **按空行自动分段**：仅在还没有标题时显示。每个空行隔开的块形成一段；首行符合短标题判断时将其变成标题，否则添加“第 N 段”标题。自动分段后仍需检查名称和完成条件。
- **并入已有阶段**：把这一段设成“并入已有阶段”，再选一个目标阶段，这一段文字就会和那个阶段一起发送，但不占新的进度位置。
- **保存**：把标题及规则标签写入世界书正文并读回校验。普通编辑会保留指导正文；选用首行作标题时会把该行转成标题，删除标题时也会整理多余空行。

改写大段正文、移动整个内容块这类操作，直接切到“编辑原文”改，或者在酒馆的世界书编辑器里改，再回管理页点击“刷新”检查结果。保存只写正文，绑定关系与聊天进度不受影响。

## 从 1.x 升级

### 已经使用旧文本模板

`【内容：名称】`、`什么时候出现：`、`告诉AI：`、`什么时候消失：` 等旧模板语法仍可解析。剧情阶段的“什么时候消失”会作为完成条件；`类型：重要物品` 等非主线类型会作为附加内容。

旧模板能被识别，不代表旧自然语言时机规则完全相同。v2.0 主线只按正文顺序推进，附加内容只按阶段范围启停：

- `《进入地下室》正在进行时`、`《进入地下室》结束后` 都只会提取书名号中的阶段名，不再区分“进行时”“结束后”的偏移；起止范围包含该阶段。
- `游戏开始时`、`上一段结束后`、`进入某地点时` 等句子不会被当作独立触发事件。用于附加范围且无法解析到阶段时，会提醒并按上述默认范围回退。
- 升级后应核对附加范围，推荐改写成明确的 `从：阶段名`、`到：阶段名`。阶段仍按顺序推进，不会依据“什么时候出现”跳转或等待事件。

### 曾经用选区高亮划分阶段

v1.3 系列的选区划分保存在条目扩展数据或正文末尾的 `DGA_LAYOUT_V1` 隐藏标记中。v2.0 起会识别可读取的旧划分，并在管理页提供“转换成新版格式”；这类条目在转换前暂停指导注入，需要先转换，才能继续使用、重新添加或进入新版编辑器。

1. 选择带有“旧版划分，需转换”提示的条目，点击“转换成新版格式”。
2. 转换会**直接改写世界书正文**：按旧阶段顺序生成 `## 阶段名` 与完成条件，把每个阶段能定位到的选区按原文顺序拼接；旧常驻选区变成 `[常驻]` 块。搬运文字中看起来像标题或规则标签的行会自动转义，避免被误识别为新阶段或规则，发送时还原原文。
3. 没有分配到阶段的原文汇总到末尾的“旧版没有分配的文字 [备注]”，不再发送。无法重新定位的旧选区不会还原成原阶段，检查时要特别留意备注和空阶段。
4. 转换会移除旧正文隐藏标记和条目中的 `extra.dynamicGuideAssistant` 数据。阶段颜色由新版按顺序分配，不保留旧版自定义颜色。
5. 打开“划分阶段”检查内容、完成条件与顺序，再点“保存并添加”或回管理页“重新添加”。重新添加后当前聊天从第一段开始；转换动作本身不会重置聊天进度。

新版不再使用或恢复角色变量、全局变量中的旧 `layouts` 备份，也不再把划分写回隐藏选区数据。若旧扩展数据和正文隐藏标记都已丢失，需要先在旧版恢复划分，或在新版直接重新添加标题。

## 进度与运行方式

- 阶段、附加范围、完成条件都在世界书正文里；角色变量 `$dynamicGuideAssistant.config` 保存绑定列表 `{ version: 2, bindings: [{ worldbookName, entryUid, entryName, boundAt, mirrorUid }] }`。
- 每条绑定的进度分别保存在聊天变量 `$dynamicGuideAssistant.state.bindings[绑定键]`。不同聊天独立，新聊天从第一段开始，“回到第一段”只影响当前聊天里这一条绑定。
- 兼容旧版数据：2.0 的单个绑定和扁平进度会在读取时自动迁移到第一个绑定名下并写回；1.x 进度中的 `mainIndex / mainName` 也仍能识别；≤2.4 的提示词注入残留会在第一次启动时自动清理。修改正文后优先按当前阶段名称定位，找不到同名阶段时按原序号限制到有效范围；重新添加始终重置为第一段。
- 添加时关闭来源条目并读回核实，避免完整大纲经世界书原生机制发送。来源条目之后被重新打开时，脚本会在下次同步时再次关闭。点「解绑」会重新打开条目、删掉它的镜像条目和在当前聊天的进度。
- 每条绑定在同一本世界书里维护一个「（动态指导）」镜像条目：克隆来源条目的全部设置——位置（角色定义前/后、按深度插入的深度与角色、示例消息等）、顺序、关键词、概率、递归开关——只把内容换成当前阶段的切片，所以指导就显示在来源条目原来的位置，和其他世界书条目的相对顺序不变。来源条目调整位置后，镜像会在下次同步时跟着挪。卡片里“现在发给 AI 的内容”会写出这条绑定的实际位置。
- 镜像内容始终跟随进度：页面打开、添加、上一段 / 下一段、完成标记推进和切换聊天后都会立刻同步镜像；生成事件里再同步兜底一次（swipe / 重新生成时换用推进前的阶段）。同步是幂等的：内容或位置有变化才写世界书，没有变化就不写。一条回复可以同时带多条绑定的完成标记，脚本按各自的阶段 id 分别推进，并在接口可用时从回复中移除标记。
- 自动推进后立即重新生成该条回复时，脚本会尝试仍使用推进前的阶段。管理页随时可以手动调整每条绑定的进度。
- 全部阶段完成后这条绑定不再显示任何指导（镜像会被移除），包括附加与常驻内容；可以点“上一段”或“回到第一段”恢复。
- 「自动推进」是全局三档设置（管理页顶部设置卡），对没有写 `完成：` 条件的阶段生效：**手动推进**（默认）只能手动点“下一段”；**标记判断**在镜像末尾附一段通用判断指令和隐藏标记，由正文 AI 自己决定时机，不额外花请求；**判断AI**在每条 AI 回复后用 `generateRaw` 静默判定，回答 YES 才推进，每条回复多花一次小请求。单个阶段写 `完成：自动` 可跨档位单独开启 AI 判断；判断AI判定期间如果进度已被标记或手动推进、或来了新回复，这次判定会被放弃。
- 判断AI可以使用动态指导助手自己管理的 API 预设：点左上角目录按钮进入「API」页，连接方式三选（酒馆主 API / 自定义 / 酒馆预设——分别走酒馆当前主接口、直连酒馆后端转发、酒馆连接管理器中的预设）。自定义时再选接口协议四选（兼容 OpenAI / OpenAI Responses / Claude Messages / Gemini Interactions），填写端点 URL、API Key、模型名（点「加载模型」由酒馆服务器代发拉取，新建预设不用先保存）、最大回复长度和温度，并可设置附加主体参数、排除主体参数、提示词后处理、附加请求标头等高级参数。完整预设只保存在当前浏览器的 `localStorage`；角色变量只记当前选中的预设名，因此 Key 不随角色卡导出。`localStorage` 是本机明文存储，使用共享设备时不要保存敏感 Key。
- 判断AI的提示词在独立的「判断AI提示词」页按段自定义（每段可选 system / user / assistant 角色，支持占位符 `{{stage}} {{prompt}} {{condition}} {{history}}`，可一键导入导出、恢复默认）。判断时**只看 AI 最新 1 段正文**（用户消息不发送，参考段数可在「动态指导」页调成 2 / 3 / 6 / 自定义）。「动态指导」页的「提取 / 排除规则」卡与数据库填表规则同一语义：发送前逐段过滤角色回复——提取规则只保留「开始边界~结束边界」之间的内容（含边界、取最后一处命中），排除规则删掉「开始边界~结束边界」区间（含边界、支持嵌套）；解析结论前也会对判断AI的输出再过滤一次（无命中时原样保留）。规则开始和结束边界都填上会自动保存生效，同卡的规则测试器可用当前规则直接试跑样例或最近一次真实输出；留空 = 不过滤。
- 目录抽屉里的「运行日志」页实时记录判断AI检查与结论、阶段推进、绑定变更、拉取模型等事件，支持等级筛选、复制与清空；日志只存内存（上限 500 条），不写变量、不上传。

当前版本采用线性阶段，不支持分支剧情图。自动推进依赖模型遵守完成标记，复杂条件可能需要手动调整。“只显示当前内容”仅控制镜像条目本次发送的指导，不会删除聊天历史中已经出现的信息。

## 更新日志

### v2.91（2026-09-26）

- 设置页去掉大段说明。换边方向改成一排小按钮：回这条、去那边、两边。

### v2.90（2026-09-26）

- 换边改成一张卡片一行。上面标明「这条」和「那边」，中间只显示当前方向，点开再选回去、从那边过来或互通。

### v2.89（2026-09-26）

- 新建阶段的弹层里不再显示「并入」。点开已经有的阶段时，并入还在。

### v2.88（2026-09-26）

- 编辑里点「设置」不再弹出未保存确认。分段先留着，从设置返回还是刚才那篇。

### v2.87（2026-09-26）

- 依附之后可以「新增」换边。左边选当前条目的阶段，右边选被依附那条的阶段。
- ← 回去，→ 从那边过来，↔ 两边到了都能选。到了那一段会问要不要换过去。

### v2.86（2026-09-26）

- 点进一段时，不再显示「发给 AI」的预览。
- 常驻不再选择排在阶段内容之前还是之后。
- 在线加载改走 `cdn.jsdelivr.net`。上一版用的 gcore 地址连不上。

### v2.85（2026-09-26）

- 点进一段时，不再显示「到了这里」和「走完回到」。
- 不再提供附加内容：阶段页没有「附在这一段」，分配栏也没有「新附加」。
- 依附的「从第几段开始」会带上那一段的名字。
- 打开编辑时停在页顶。「编辑 / 设置」和「分段 / 编辑原文」钉在上面，往下翻也不会被卷走。

### v2.84（2026-09-26）

- 时间线整页拿掉。小卡上只留「编辑 ›」和「设置 ›」。编辑仍是分段和编辑原文。
- 不再新建剧情指导，只绑定已有条目。
- 一条可以依附另一条：选条目，再选从第几段开始，再选分岔口或支线。分岔口上还能再依附，不另做提示。没选依附的自己走。
- 走到那一幕时选：继续这条、走进分岔口（原来那条断掉）、走进支线（走完回到原来那条的下一段）。
- 分配栏不再提供「常驻」。循环仍在设置里：再选一次，或还走刚才那段。

### v2.83（2026-09-25）

- **时间线的线接到卡片上。** 从一张卡片底边折到下一张顶边。卡片加宽，标题不再挤成一竖条。

### v2.82（2026-09-25）

- **时间线改成分叉图。** 点一张卡片，再选往下接一段、从这里分开、转到另一条，或标成支线。支线是从这条线出去走到头。转线是走到这一步可以换到另一条线。

### v2.81（2026-09-25）

- **新建剧情指导的位置和酒馆一样。** 角色定义前后、示例消息前后、作者注释前后、系统/用户/AI 插入深度、锚点，都可以选。选了深度要填层数，选了锚点要填名字。

### v2.80（2026-09-25）

- **可以新建剧情指导。** 自己定放在世界书的哪个位置、顺序是几。全文那条关着。开着的小条只放当前这一段。
- **在脚本里改完保存，全文按阶段顺序写回。** 先改后面再补前面，前面会插在它该在的位置。

### v2.79（2026-09-25）

- **时间线改成一条往下的路。** 卡片不拖。每一站可以「在后面加一段」，并排可以「再加一条路」。点开卡片改字，最底下「删掉这段」，字回到未分配。
- **走完可以回到前面某一段。** 点开一段，选「走完回到」。标了「到此结束」的段不回去。

### v2.78（2026-09-25）

- **几条路并列。** 默认「接着往下」，写明走完去哪一段。点「分成几条路」后，路的名字一样大，正在改的那条标出来。读者只能点一条，没点到的这次不走。走完都到后面同一段。

### v2.77（2026-09-25）

- **走到这里先看懂再选。** 默认是「接着往下」：走完这一段，就去下一段。点「从几段里挑一段」之后，才把别的段放进来。黄的写「在前面」，蓝的写「就是这段」，绿的写「在后面」。这几段里只能走一段。

### v2.76（2026-09-25）

- **岔路用颜色分前后。** 前面的段是黄的，写着「之前」。正在改的这段是蓝的，写着「同时」。后面的段是绿的，写着「之后」。下拉框里也先写上之前或之后。

### v2.75（2026-09-25）

- **岔路改成大白话。** 点进某一段，看到「走到这里要选一段」。只列出可以选的几段。再加一段、不要这段，都写在按钮上。
- **时间线不再一堆斜线。** 一组先收成一根横线，再分到下一组要选的几张。

### v2.74（2026-09-25）

- **岔路只显示可以选的段。** 点进某一段后，岔路里只列出已经和它互斥的段。其他段从「加上一段」加进去，移出就不再一起选。

### v2.73（2026-09-25）

- **常驻不在点进某一段的页面里加。** 常驻仍在分段里把文字标成常驻。
- **岔路改成勾选。** 点亮要和这一段互斥的其他段，不用再手写同一个分支组名。都不点就是普通往下走。

### v2.72（2026-09-25）

- **点进一段不再挤成一堆。** 分成离开这一段、岔路、发给 AI、附在这一段、这一段前后的常驻、整理。
- **这一段可以带自己的附加和前后常驻。** 发给 AI 的预览按「之前的常驻、这一段、附加、之后的常驻」排，全局常驻也会算进去。

### v2.71（2026-09-25）

- **循环绕回时走哪条，创作者先定好。** 阶段怎么走选了「循环」之后，设置里可以选「走同一条」或「重新选择」。绕回的时候不再询问。没改过的仍是重新选择。

### v2.70（2026-09-25）

- **时间线、编辑、设置里面可以互相去。** 小卡上仍是三个入口。进了其中一页，顶上有「时间线 / 编辑 / 设置」，不用先退回小卡。
- **阶段怎么走和分支组各管一件事。** 按顺序、循环、AI 选段管这一轮怎么推进。分支组是岔路。循环绕回第一段时，分支选择清空，这一轮重来。

### v2.69（2026-09-24）

- **时间线能打开。** 画箭头时不再给 SVG 写只读的 className，点「时间线 ›」不会把面板弄卡。
- **分段里的原文优先发给 AI。** 导图时期留下的空卡片正文不再把这一段发成空白；原文里已经有字时，旧卡片正文不再盖过它。只有卡片里有字、原文还没划进来时，分段里能看到这段字，也会发给 AI。

### v2.68（2026-09-24）

- **时间线不再能拖。** 小卡上的「时间线 ›」按阶段顺序自动排出箭头，只用来看。编辑仍是原来的分段和编辑原文。

### v2.67（2026-09-24）

- **可选还是互斥，点线上的字再选。** 编辑卡片只写这一张的剧情和连到哪。线连上之后，图上那根线中间写着「可选」，点它改成「互斥」，也可以再点要一起作废的其他线。

### v2.66（2026-09-24）

- **导图上的线会画出箭头。** 上一版的线浏览器不认，所以看起来没有。点卡片，编辑，加一条线，保存之后，两张卡片之间会出现一根带箭头的线。可选的线是虚线。

### v2.65（2026-09-24）

- **点卡片可以选择编辑或删除。** 编辑里写这一张自己的剧情，可以写成完整的一段，也可以只写这一步，留空就只当一个步骤。写在卡片里的字会成为走到这张时镜像的正文，不改原来的大纲条目。
- **从一张卡片拉出去的线可以单独设。** 可选的是支线，走了不取消别的线。互斥只和你点名的那几根打架。

### v2.64（2026-09-24）

- **「划分」改成「编辑」，打开是一张导图。** 可以新建卡片、拖动摆位置、点 × 删除。点卡片仍改名称和什么时候离开。原文还是只有在「编辑原文」里保存才会改。
- **小卡上多了「时间线 ›」。** 能看到全部阶段，当前这一张会标出来，走过的、这次不走的分支也能看见。时间线里不能新建、拖动或删除。

### v2.63（2026-09-24）

- **阶段可以写「分支：组名」。** 同一组名的阶段互斥：进入其中一个后，其余分支这次聊天就不再走、不再发送，小卡和编辑器里会标出「分支·组名」。走到分支时：手动点「下一段」会弹出走向选择；随正文 AI 会给每个走向各一行标记，回复带了哪行就进哪条；判断 AI 判定当前段完成后会补问一次走向（对不上就不推进，下次再查）；AI 选段的目录里被否决的分支不再出现。循环绕回时分支选择清空、全部重来。
- **小卡上的上次结论字多了会缩略。** 超过一行的依据只显示开头，点「展开」看全部，再点「收起」。依据长度上限从 60 字放宽到 500 字。

### v2.62（2026-09-23）

- **basis 和 verdict 改成与 checklist 一样的条目。** 标签里面写 `- 已发生：…`、`- 结论：…`，不再是空标签。AI 选段的 stage 同样写成 `- 序号：…`。改过判断提示词的，要点「恢复默认提示词」。

### v2.61（2026-09-23）

- **每条绑定可以选阶段怎么走。** 在「设置 ›」里三选一：按顺序、循环、AI 选下一段。AI 选下一段会看正文现在像哪一段，可以从第 3 段跳回第 1 段，不必一格一格往后。小卡上会标「AI选段」或「循环」。划分页顶上的按钮也是同一个开关。

### v2.60（2026-09-23）

- **默认判断改回通用口径。** 不再把某一张卡的暑假、平时写进提示词。规则只剩一条：一段时间或持续状态还停在当前这段，就写 NO；正文已经换成下一阶段，才写 YES。最后只留一张空表，不再解释标签是干什么的。改过判断提示词的，要点「恢复默认提示词」。

### v2.59（2026-09-23）

- **判断空表改成英文标签。** 默认输出是 `<basis>` 和 `<verdict>`，给 Gemini 照着空表填，标签名不要翻译。basis 里仍用中文写发生了什么，verdict 里只写 YES 或 NO。填之前先按清单对一下当前时间。旧的 `<依据>` `<结论>` 还能认。改过判断提示词的，要点「恢复默认提示词」。

### v2.58（2026-09-23）

- **默认判断先看正文现在过的是哪一段时间。** 暑假、寒假、平时这种阶段，正文还停在这段就写 NO。在家、不用上课，是暑假还在继续，不会因为「符合暑假」换成平时。要看到已经开学、去上课，才离开暑假。吃一顿饭、看一会儿电视，不换时间。状态栏、总结、思维链里的字不算。判断时会带上一段的名字和内容。改过判断提示词的，要点「恢复默认提示词」才会用上这套。

### v2.57（2026-09-22）

- **「划分 ›」放到「设置 ›」左边。** 两个都在上一段、下一段下面。设置再往右一点。

### v2.56（2026-09-22）

- **小卡不再写检查间隔。** 每层都查、还差几层，都不显示。隔几层仍在「设置」里改。有上次结论时，小卡只留那一句。

### v2.55（2026-09-22）

- **待选列表不再出现插件自己的条目。** 动态指导的镜像、标记、配置、状态条目不会出现。数据库写进世界书的 `TavernDB-ACU-`、总结条目、重要人物条目，以及带导出标记的隐藏数据，也不会出现。这些条目本身不动。

### v2.54（2026-09-22）

- **板块标签贴在标题下面。** 不再跟在「绑定世界书」卡片后面，也不会被卡片盖住。字更小，当前这一项下面有一条细线。
- **搜索框跟上配色。** 不再是一条白底。
- **每条的判断设置收成「设置 ›」。** 放在上一段、下一段下面，点开再改判断方式和隔几层。

### v2.53（2026-09-22）

- **小卡上可以现在检查。** 判断 AI 不用再等攒够层数。可以写一句只对这一次生效的附加要求。
- **每条绑定可以单独选判断方式和隔几层。** 没单独写的，仍跟着「如何判断」里的全局设置。小卡上能看到上次结论，以及还差几层才检查。
- **条目可以搜索。** 世界书默认打开这张卡绑定的那本，其他书也在列表里，角色卡的会标出来。
- **动态指导页顶部可以跳到绑定、如何判断、提取规则。**

### v2.52（2026-09-22）

- **选中条目不会按标题自动拆成阶段。** 下拉框默认停在「请选择条目」。选中以后，正文里的 `##` 标题还是普通文字。点「划分阶段」，自己拖选分配。已经保存过的划分照旧可用。以前已经绑定、正文里用 `##` 分好段的老条目，打开后仍会接着发送。

### v2.51（2026-09-22）

- **划分改成和数据库一样存。** 写进酒馆扩展设置，再在角色变量里留一份。世界书列表里不再多一条「（动态指导·状态）」。原来写在条目隐藏字段或那条状态条目里的，打开后会搬过来，然后把看得见的状态条目删掉。原文不动。

### v2.50（2026-09-22）

- **划分和完成条件跟循环一样写进世界书状态条目。** 隐藏字段被清掉后，下次打开会写回去。原文不动。
- **绑定按「从第几步开始」进入当前聊天。** 不再一律从第 1 步写进度。
- **诊断用和正文同一条路径比较镜像。** 不再拿已经删掉的标记判断去对正文。

### v2.49（2026-09-22）

- **同一张卡、同一台手机上，循环不会再自己关掉。** 一点「循环」就写进世界书里关着的「（动态指导·状态）」，不用等保存。酒馆把条目隐藏数据清掉、或者条目编号变了，下次打开也会从这条把循环找回来。

### v2.48（2026-09-22）

- **导出不再带 API 预设名。** 判断 AI 和生成完成条件用的预设名只留在这台电脑。角色变量和世界书配置里已经写过的，打开这个版本后会搬回本机。
- **开发者模式里可以填「导出后从第几步开始」。** 新聊天和导入这张卡的人从这一步开始，不一定是第一段。当前这次聊天的进度不动。
- **循环记在绑定上，跟角色卡走也记在这张卡上。** 不再只靠世界书里那份容易丢的隐藏数据。浏览器里的「跟卡走」丢了，但世界书里还有配置条目时，会自动恢复。

### v2.47（2026-09-22）

- **电脑端按数据库那样分布。** 左侧常驻目录，右侧是当前页。宽屏上仪表盘和动态指导的卡片分成两列。窄屏仍用左上角 ☰ 拉开目录。划分阶段、判断 AI 提示词仍是二级页，不放左侧目录。

### v2.46（2026-09-22）

- **电脑上不再是居中小窗。** 面板铺满屏幕，和数据库一样。

### v2.45（2026-09-22）

- **手动推进不调用 AI。** 只能自己点上一段、下一段。
- **随正文 AI 判断：写正文的 AI 在同一条回复里带标记。** 这一段完成了，回复末尾会有一个标记，脚本看到就进入下一段。不再另开一次请求。标记说明单独放在「（动态指导·标记）」，不改原文，镜像仍是原文切片。

### v2.44（2026-09-22）

- **绑定的原文不再被插件改写。** 划分、完成条件、循环只记在条目旁边的隐藏数据里，随角色卡的世界书走。正文 AI 读不到这份数据，判断 AI 会读。
- **镜像只复制当前阶段的原文切片。** 不再附加完成条件、完成标记或插件标题。
- 「编辑原文」仍是给你自己改字用的。只有在这一页改过并保存，原文才会变成你写下的内容。

### v2.43（2026-09-22）

- **流式输出这类开关不再带酒馆的对勾。** 打开后只剩滑动的圆点。
- **循环开着时，进度停在「全部完成」会回到第一段并继续发送。** 小卡不再写「不再发送指导」。保存循环后回到动态指导页会按保存结果刷新。

### v2.42（2026-09-22）

- **点「划分」后顶栏不再跑出屏幕。** 长正文只在面板内部滚动，不再把酒馆上半部分一起卷走。

### v2.41（2026-09-22）

- **循环在小卡和快捷指令上都能绕回去。** 最后一段的「下一段」回到第一段，第一段的「上一段」退到最后一段。小卡会标出「循环」；标了「到此结束」的阶段也会写在段数旁边。
- 判断AI还在问的时候又来了新回复，问完会补判最新一层，不再丢掉。多条绑定同时判断。判断回复压到 1024 字，避免预设里的超长上限让模型空转。
- 划分阶段页会显示已经划好的阶段数，不依赖原文里有没有 `##` 标题。

### v2.40（2026-09-22）

- **「编辑原文」里改过的字会保存。** 已经划好的阶段跟着文字挪，后面的阶段不会被带跑。
- 判断AI还在等结果时，手动点的上一段/下一段不会被盖掉。同一层重新生成会再判断一次。
- 附加内容的「第几段」跟 ↑↓ 定下的推进顺序一致。最后一段的 ↓ 不再假装还能点。两个阶段同名时，进度停在当前这个，不跳回第一个。循环回到第一段之后，重新生成仍显示刚完成的最后一段。

### v2.39（2026-09-22）

- **目录里的页面恢复左上角导航。** API、动态指导、运行日志、开发者模式都可以打开目录换页。划分阶段、判断AI提示词这类钻进去的页面仍然没有导航，右上角是 ×。

### v2.38（2026-09-22）

- **「AI 生成」改成写进入下一阶段的条件。** 暑假的下一段是寒假时，生成「正文已经写到寒假开始」，不再生成「还在暑假」。剧情阶段则写下一段里那件标志事情已经发生。回复长度压到约 48 字，并关掉这一次的流式输出，减少干等。
- 判断时，寒假开始、暑假结束、开学这类时间条件，只看正文有没有写到那个时间。
- 若之前在齿轮里保存过旧的生成提示词，点一次「恢复默认」才会换上这套。

### v2.37（2026-09-22）

- **没有 `##` 标题也能绑定**。选中条目后，「划分阶段」和「绑定」一起出现。可以先绑定，再在小卡上划分。
- **划分阶段不再改原文**。拖选、新建阶段、↑↓ 调顺序，都不会给原文加字、删字或换位。顺序只决定下一阶段进哪一段，以及镜像里发哪一段。以前被改乱的条目，需要你自己把原文贴回去再分一次。
- **已分配的文字可以再分配**。再拖选那段文字，选另一个阶段，就会并过去。阶段属性里也可以选「并入」另一个阶段。
- **「AI 生成」收短并认时间**。寒暑假、学期、周末这类阶段，生成的是「正文已经进入暑假」这种能核对的时间结果，并且把回复长度压到很短，减少干等。
- **某一段可以到此结束**。阶段属性里选「到此结束」，到了这一段就不再判断要不要进入下一段。
- **阶段可以循环**。分段视图里打开「循环：开」。走完最后一段之后，下一次会回到第一段。寒暑假那种暑假 → 寒假 → 平时 → 再回暑假，用这个。

### v2.36（2026-09-22）

- **二级页改成数据库那种头**。动态指导、API、运行日志、判断AI提示词、开发者模式、划分阶段，左上角不再放导航按钮，右上角只留一个 ×，点它回到上一页。仪表盘仍是左上角导航、右上角外观齿轮和关闭。
- **绑定改回以角色卡自己的变量为准**。v2.32 用头像文件名在浏览器里另存一份，会和角色变量打架，改头像还会对不上。现在正本就是这张卡的角色变量，换卡天然换一份。「只本机」不再写进世界书；「跟角色卡走」才额外写一份脱敏配置，给导入的人接上。以前按头像存过的那份，只在这张卡还没有角色变量时读一次。

### v2.35（2026-09-22）

- **仪表盘标题下的小字去掉了**。不再显示版本号和当前角色名，标题只留「仪表盘」。
- **右侧外观按钮改成齿轮**。原来的 🎨 换成 ⚙，点开仍是配色面板。

### v2.34（2026-09-22）

- **「AI 生成」的完成条件收成一道窄闸门**。它只写一件收尾时能在正文里看见的结果，16 到 40 字。过程、气氛、心理、原句对白和精确次数都不写进去，避免判断时被拆成一堆必须逐件发生的硬条件。仍然拒绝「加深羁绊」这类抽象判词。
- **编辑器齿轮里可以自定义这套提示词**。点右上角 ⚙，再点「自定义生成提示词」。系统段和用户段都能改，支持 `{{stage}}` 和 `{{prompt}}`，可以恢复默认。这里的提示词和判断AI的提示词分开。
- **生成可以单独选 API 预设**。同一个窗口里选「酒馆主 API」或已有预设，也能点「管理 API 预设」去新建、改地址和密钥。选中的预设名不跟角色卡导出。从 API 页返回会回到编辑器。

### v2.33（2026-09-22）

- **判断提示词按「能不能在正文里核对」重写**。四段结构还在：系统契约、判断规则、预确认、最后一段才是这一轮的案子。已发生的事只认「最近演到哪了」；提到、计划、回忆和用户的台词都不算做成。只有条件里的每一件都有原文依据才写 YES，做了一部分或走偏了都写 NO，并写明差在哪。
- **没写完成条件时不再用「充分展开」放行**。以前这句几乎会让每一层都被判成可以进入下一段。现在改为核对这一阶段正文里写出的具体情节是否已经发生。标记判断那一行给正文模型的说明也改成同一标准。
- **「AI 生成」完成条件改成可打勾的完成态**。它写的是以后能对照正文判断真假的结果，不是把这一段现在的剧情再摘要一遍，也不接受「加深羁绊」这类抽象判词。已经改过提示词的人，到「判断AI提示词」页点一次「恢复默认提示词」才会换上这套。

### v2.32（2026-09-22）

- **只本机改为按角色卡分开存**。以前这一档是这台浏览器共用的一份：绑了 A 卡再打开 B 卡，仍会看到 A 卡的绑定。现在本机档的键里带当前卡的头像文件名，换一张卡就读另一份。A 卡已经存下的绑定还在，只是 B 卡看不到。认不出当前是哪张卡时，不再退回成全库共用的那一份。
- **自愈只看当前这张卡绑定的世界书**。以前启动时会把库里所有世界书的「（动态指导）」镜像都收进当前配置。现在只接管挂在这张卡上的世界书；换聊天、换卡时也会再扫一次。别人的卡里的镜像留在原处。
- **跟角色卡走的配置条目不再带着关键词**。写进世界书的「（动态指导·配置）」会强制关闭，并清掉从别的条目克隆来的关键词和常驻开关，避免被重新打开后把配置发给 AI。正文特别短、对不上行的时候，自愈会按镜像里的「当前阶段：阶段名」对齐进度。

### v2.31（2026-09-22）

- **跨卡导入会自己接上**：别人发来的角色卡会带上世界书，但绑定列表存在角色变量里，不一定跟卡一起过来。世界书里如果已经有「条目名（动态指导）」镜像、当前却没有对应绑定，启动时会按镜像名找回原条目、重建绑定，并拿镜像正文去对阶段正文，对齐到现在这一段（对不上就从第一段开始）。主动解绑会把镜像一起删掉，所以刚解绑的条目不会被重新绑上。
- **配置可以选择存在哪**：仪表盘「开关」里原来的「暂未开放」改成「高级设置」。打开「开发者模式」之后，左侧导航会多出一页「开发者模式」，在那里切换存放位置。
  - **只本机**（默认）：和 API 预设同一层，存在这台浏览器里。换任何角色卡都还在，也不会跟着卡发给别人。绑过 A 卡再打开 B 卡时，仍会看到那条绑定，失效时显示「绑定已失效」，可以解绑。
  - **跟角色卡走**：额外把一份配置写进当前世界书的「（动态指导·配置）」条目。这个条目是关着的，不发给 AI，也不参与关键词。写出去之前会去掉 API 预设名（它只是本机某个预设的名字，预设本体里的地址和密钥本来就不在配置里）。
  - 两档都会再写一份到角色变量。读取顺序是本机档 → 角色变量 → 世界书配置条目，所以换档、旧卡、别人发来的卡都还能读到配置。
- **外观收进右上角**：仪表盘上的「外观」卡挪到标题栏右上角的 🎨，点开才是配色面板，不再占正文位置。仪表盘标题改成「仪表盘」，不再把插件名再写一遍（插件名仍在左侧导航里）。默认配色仍是偏黑藏青。

### v2.30（2026-09-21）

- **修掉一个会改动原文顺序的 bug**：分段时只该换归属、不该动先后，但重建正文时是按「未分配前言 → 阶段 → 附加 → 常驻 → 备注」的**固定分区顺序**输出的，于是常驻被强制排到阶段之后（或之前），一旦它的文字本来排在别处就会整块被搬走。典型表现：先把第二行分给一个阶段、再把第一行标成常驻，回来一看两行对调了。现在改成**按文字位置排放**，每个分段落在它名下第一段文字的起点；常驻块的「位置」开关只保留「排到最前」这一种显式意图，不再靠"强制排最后"来表达默认状态。附加文字排在阶段之前时同样不再掉到后面。
- **判断AI提示词按数据库（SP·数据库）的做法重写**。先说清楚一件事：数据库**没有**「单阶段是否演完」这种一一对应的判定提示词——它的剧情推进是编排与注入，不是判定。真正承载它有效性的是另外两样，这两样被搬了过来：
  - **认识论边界**（它的结算维护代理与各 Agent 提示词里反复出现的硬线）：已发生事实的唯一来源是真实历史；**被切换掉的重写、被删除或被编辑替换的内容一律不算发生过**；大纲与计划不是事实；提到、商量、计划、预告、假设、否认、回忆都不算发生；信息不足时明确写缺口，**绝不用「听起来合理」的细节把空白填上**。
  - **达成度三档**（它的结算交付判定）：**达成 / 部分达成 / 偏离**。现在只把「达成」映射为 YES，「部分达成」与「偏离」都写 NO，并要求在 `<依据>` 里点出还差什么、或实际演的是什么。
  - 手法上也对齐：assistant 预确认段逐条复述口径（数据库大量使用这种 ACK 段）、案例段末尾挂**【自检清单】**逐条自问（数据库每个契约段都带自检清单）、system 段补负向边界（"你不写剧情、不续写、不评价文笔、不改大纲"）。
- **新增「AI 生成」完成条件**：编辑器里点开任意阶段（或新建阶段时）的属性弹层，「什么时候进入下一段」下面多了个 **AI 生成** 按钮，点一下就让模型读着这一段的正文写一句完成条件，填进草稿等你确认（点「保存修改」才落盘）。用的是**判断AI那一套 API 设置**（没设预设就落到酒馆主 API），提示词同样按数据库的口径要求：写「已经发生的事」不写「应该发生的事」，**拒绝「推动剧情发展」「加深羁绊」这类空泛判词**，只写可核对的既成事实，一件事一句话。
- **修掉输入框和藏青不配的问题**：输入框底色原本是「面板底 + 12% 近白」，而**往白里混会把藏青洗成灰**——`#0E1523` 混出 `#282F3C`，饱和度从 43% 掉到 20%，于是面板是深蓝、输入框是灰，并排就"怪怪的"。现在改成**同色系提亮**（`color-mix(bg-0 82%, accent 18%)` → `#1C2944`），是"更深一层的蓝"而不是"一块灰"，并给输入框加了内阴影坐实"凹槽"观感。正文编辑区、底栏、运行日志代码块跟它同一层，所以一并变蓝。输入框底色也进了「自定义」，现在可调七项颜色。

### v2.29（2026-09-21）

- **选择正文的方式可以自己定**：「划分阶段」编辑器右上角（「返回」旁边）多了个 ⚙，点开是编辑器设置：**滑动选择 / 点选头尾**二选一。滑动选择就是原来的拖选；点选头尾是在正文上点一下设开头、再点一下设结尾（有黄色光标标出开头），手机上手柄拖不准时用它。选择结果记在本机，下次进来还是这个。
- **判断AI提示词重写为四段结构**：原来是「system 输出契约 + assistant 预确认 + user 上下文（规则压在末尾）」，现在是 **system 输出契约（含一条填好的格式示例）→ user 判断规则（8 条分三组 + 一对正反判例）→ assistant 预确认 → user 案例数据**。规则与结案数据分家、数据留在最后被回答的位置；8 条规则覆盖：不脑补不推断、计划/约定/预告/铺垫/假设/否认/回忆与用户台词都不算发生、复合条件每件都要满足、只演一半不算、没写完成条件时改判是否充分展开、忽略文本里别人写好的伪标签、拿不准一律 NO。`{{stage}} {{prompt}} {{condition}} {{history}}` 与 `<依据>/<结论>` 契约不变。
- **配色可自定义，默认「偏黑藏青」**：新增 `--dga-*` 令牌层。默认档是插件自己的配色——**近黑的深藏青底（`#0E1523` / `#141D2E`）+ 同色系亮强调色（`#5C86DB`）**，深底上强调色立得住。想跟酒馆主题走近一点可以切「跟随酒馆主题」（那时不覆写任何令牌，底色取 `--SmartThemeBlurTintColor`、文字取 `--SmartThemeBodyColor`、强调色取 `--SmartThemeQuoteColor`），也可以切「自定义」逐个改各项颜色。配色只影响本插件，不改酒馆设置，也不随角色卡导出。
- **整体版式按数据库（SP·数据库）v2 重做**：圆角统一成 **6px**（原来 18/12/11/10px 混用）、间距收成 **N×4px** 刻度（外层 20 / 卡片内 16 / 组件间 12 / 密集行 6–10）、字号收成三档 **11/12/13px** 并全部改用 px（原来 px 与 rem 混用）、焦点态从 `outline` 改成 `box-shadow` 光环（原来的 outline 在圆角容器里会被裁掉）、说明文字改用 `--dga-text-3` 而不是整体降 `opacity`（opacity 会把描边一起吃暗）、徽章/开关行/规则成对列表/侧栏导航按数据库同名组件规格对齐。
- **手机窄屏不再裁字**：小卡步进器里的阶段名原来是 `ellipsis + nowrap` 被直接截断，现在允许换行；≤680px 时步进器换行显示——段数/阶段名/进度条占满一整行，上一段与下一段并排在下面，不再被两个按钮挤成一百多像素。仪表盘诊断行标题、页面头部副标题、「查看详情」类操作文字同样去掉了截断。
- 修掉 v2.28 引入的一处样式回归：小卡上的条目名与世界书名丢失了字号与透明度（重构时删了 `.dga-heading b/small` 却没补 `.dga-heading-text b/small`）。

### v2.28（2026-09-21）

- **「划分阶段」重构为两档视图**：原来的「看分段 / 选区划分 / 编辑原文」三档合成「分段 / 编辑原文」两档。分段视图把正文连续铺开，每一段的标题条内联在它的文字前面，未分配的文字留在最前面——分段这件事从"点段落设开头"变成"拖选文字 → 选归属"。
- **唯一属性弹层**：名称、类型（剧情阶段 / 附加内容）、进入条件、生效范围、常驻前后位置、删除全部收进一个弹层。原选区模式那套 chips 侧栏 + 属性表单整块删除，同一份数据不再有两套编辑界面。
- **归属下拉管全部**：拖选后底部浮出的下拉一次覆盖「未分配（不发送）／各阶段／各附加／常驻提示／备注／＋ 新阶段…／＋ 新附加…」。原来的「分配给」「点选头尾」「移除选中段」「清除」四个按钮全部删除——取消分配变成下拉里的一项，新建也走同一条路（只能从选中的文字建，不再有「+ 阶段」造空壳）。
- **初始就是不分段的原始正文**：新条目打开后整篇标为「未分配」，由用户自己划；一个标题都没有时会提示「还没有分段」，另留一个「按空行先切成块」的快捷按钮。
- **「划分阶段」入口并入小卡步进器**：点小卡步进器中间那块（第 X/N 段 + 阶段名 + 进度条）即打开编辑器，并直接落在这一条当前的段上（滚过去 + 高亮一次）。于是不同小卡进去分的就是各自条目的段，不再依赖"添加行里下拉选中的是谁"。添加行只剩一个按钮，按状态变形：未分阶段 →「划分阶段」，已分阶段 →「绑定」，已绑定 →「已绑定」。
- **正文成为唯一真相**：结构改动一律「改派生模型 → 落回正文 → 重新派生」，`stale`／未重建态／保存前重建那一整套中间态机制删除；「保存并添加」一并去掉（保存语义不再分叉，未绑定的条目保存后会提示回上一页点「绑定」）。
- **「合并到」不再有 GUI 入口**：它本来就没有选区模型（选区载入时并入块的正文已经折进目标阶段）。已有「合并到」的条目照旧解析与发送，但在「分段」视图保存会把那处结构摊平成目标阶段的普通正文——检测到就会挂一条警告条，要保留结构请用「编辑原文」。

### v2.27（2026-09-21）

- **绑定即认领**：「绑定世界书」卡改成一条待绑行——选中条目点「绑定」后，这一行当场被认领清空，条目立刻变成上面的常驻小卡并淡入高亮一次，看得见它搬到哪去了；想连绑多条接着选下一个即可，不再需要一个条目同时出现在小卡和待选行里。
- **取消 ＋ / － 与删除模式这层行管理**：行不再滞留，多行管理失去对象；已绑定条目改由小卡右上角常驻的 × 解绑（二次确认保留），不必先开删除模式。
- **绑定不再弹确认框**：绑定是非破坏性的、随时可以 × 解绑回退，弹窗只会让连绑变成反复确认；「条目会被关闭」写在卡片提示与运行日志里，解绑（会删掉当前聊天进度）仍然保留二次确认。
- 下拉里已绑定条目统一标成「（… · 已绑定）」，选中后按钮显示不可点的「已绑定」。
- **全界面文案瘦身**：删掉重复解释与第二人称长句，说明压到一行以内。最长的「提取 / 排除规则」说明从 126 字压到 46 字；「规则测试：用当前规则试跑一段文字」→「规则测试」，「填入最近一次判断AI输出」→「填入最近输出」，规则行的「提取开始边界 / 提取结束边界」→「开始边界 / 结束边界」（分组标题已经写明提取还是排除）；「选择一个世界书」→「世界书」，「绑定一个或多个条目」→「条目」。API 页、判断AI提示词页、运行日志页、编辑器同步瘦身，判断AI提示词正文（发给模型的部分）保持原样不动。

### v2.26（2026-09-21）

- **已绑定条目常驻显示**：「绑定世界书」卡顶部新增常驻小卡列表——只要绑定还在，就一直显示条目名与世界书名，不再依赖添加行里选没选中它。
- **上一段 / 下一段步进器小前端**：‹ 上一段 ｜ 第 X / N 段 + 阶段名 + 细进度条 ｜ 下一段 ›，下一段在主色调高亮；全部完成时显示「全部 N 段已完成」。
- 删除模式同步覆盖常驻小卡：小卡头部的红减号 = 解绑（自带确认弹窗即二级提示）；添加行选了已绑定条目时按钮显示「已绑定」不可重复绑。

### v2.25（2026-09-21）

- **独立绑定卡片区删除**（截图红圈）：绑定卡片整块移除，功能并入「绑定世界书」卡的行内——每行「条目下拉 + 划分阶段 + 解绑」（截图绿圈：原「已绑定」改为可点的「解绑」）。
- **当前段行下加上一段 / 下一段**（截图黄圈）：已绑定行下方的当前段落行内直接带手动推进按钮。
- **减号删除模式**：点底部 － 后每行前面出现红减号，点哪个删哪行；已绑定的行删除会先弹确认（删除即解绑，二级提示），取消则行保留。
- 失效绑定（条目被删或改名）在卡内保留解绑出口；「现在发给 AI 的内容」折叠、回到第一段、立即关闭条目等随绑定卡一并移除（条目被重新打开时同步仍会自动关闭）。

### v2.24（2026-09-21）

- **仪表盘「开关」删掉判断模式三档**：只剩流式输出；手动 / 标记 / 判断AI 的切换统一在「动态指导」页「如何判断？」卡。
- **绑定世界书卡按手稿改多行**：＋ / － 按钮增删条目行，每行独立「划分阶段 / 绑定」按钮，可一次绑定多个条目；已绑定的行下方直接显示这条绑定的当前段落（第几段 · 共几段 — 阶段名）。
- **删除「三步上手」卡**；动态指导页顶部不再显示绿色成功 / 提示条（错误条保留）。

### v2.23（2026-09-21）

- **动态指导页按手稿重排**：绑定世界书（原「添加指导条目」卡）→「如何判断？」→「提取 / 排除规则」；「运行诊断」按钮与诊断卡删除（诊断能力仍挂在脚本 `diagnose()` 接口上），「刷新」按钮一并移除。
- **判断模式选择进「如何判断？」卡**：手动 / 标记 / 判断AI 三档直接在本页切换；只有切到「判断AI」档才会出现 API 预设、多久检查一次、参考几段角色回复与判断AI提示词入口。
- **提取 / 排除规则与规则测试搬到动态指导页**：开始和结束边界都填上会自动保存生效，不再走提示词草稿；规则测试器用当前规则试跑。提示词二级页只留提示词段编辑与导入 / 导出 / 恢复默认；导入文件里带规则时会直接写入设置立即生效。
- **目录导航顺序调整**：仪表盘 / API / 动态指导 / 运行日志。

### v2.22（2026-09-21）

- **目录抽屉删掉「划分阶段」**：编辑器保留，从「动态指导」页条目的「划分阶段」按钮进入；导航只剩 仪表盘 / 动态指导 / API / 运行日志。

### v2.21（2026-09-21）

- **判断AI提示词页归入动态指导**：提示词二级页的「返回」改回落到「动态指导」页（原来回仪表盘），与「判断AI」卡形成完整闭环。

### v2.20（2026-09-21）

- **判断AI设置挪进动态指导页**：API 预设、多久检查一次、判断时参考几段角色回复、判断AI提示词入口从仪表盘移到「动态指导」页的「判断AI」卡（非判断AI档时显示切档提示）；仪表盘「开关」只留流式输出与自动推进三档。

### v2.19（2026-09-21）

- **动态指导独立成页**：仪表盘只留「运行概览 + 开关」（和数据库 DashboardPage 一致）；绑定卡片、三步上手、添加指导条目、运行诊断全部移到独立的「动态指导」页（目录抽屉第二项）。运行概览「当前显示」行新增「查看指导 →」直达；「保存并添加」「关闭划分阶段编辑器」后改为落在动态指导页。

### v2.18（2026-09-21）

- **仪表盘对齐数据库前端**：状态卡改为数据库 DashboardPage 同款「运行概览」健康项——图标圆块（✓/!/×）+ 标题摘要 + 右侧徽章（已配置 / 未配置 / 需要处理 / 正常 / 无报错 / N 条报错）+ 跳转按钮（配置 API、查看日志）；API 行会检查预设是否存在、自定义连接缺不缺端点/模型、酒馆预设选没选连接预设。
- **流式输出开关**（数据库 streamingEnabled 同款）：「开关」卡基础设置页签新增「开启流式输出」开关行（标题 + 开关 + 常驻描述）。开启后：自定义连接请求体 `stream: true` 并聚合 SSE 响应（OpenAI delta 与 Claude 原生 content_block_delta 都认）；酒馆主 API 通道 generateRaw 带 `should_stream`；酒馆预设通道不支持流式。默认关闭。
- 设置卡更名「开关」并加面板描述，版式对齐数据库开关面板（AcuSegmentedControl 页签 + ToggleRow 开关行）。
- 单测 85 → **90/90**（新增：流式归一化、SSE 聚合、自定义流式请求、主 API should_stream 开/关）。

### v2.17（2026-09-21）

- **仪表盘新版式**：顶部新增状态卡——「API 设置：目前 API 是 xxx」（点击直达 API 页）、「当前显示：第 N 段 · 阶段名」（多条绑定时标注条数）、「运行日志：正常 / 警告 N 条 / 错误 N 条」（点击直达日志页）；设置卡改为「基础设置 / 暂未开放」页签。卡片顺序：状态卡 → 设置 → 绑定卡片 → 添加条目 → 诊断。

### v2.16（2026-09-21）

- **界面精简**：删除「＋ 排除思维链」「＋ 只留结论段」两个快捷按钮（规则仍可手动添加）；「规则测试」从规则卡里拆成独立卡片，分区更清楚；提示词页、设置卡、管理页的长段说明文字全面压缩成一两句。
- 功能与规则语义不变，仅界面与文案调整。

### v2.15（2026-09-21）

- **判断只看 AI 最新正文（语义修正）**：发给判断AI的「最近演到哪了」改为**只取 AI 发的正文，用户消息、系统消息一律不发送**，默认只看**最新 1 段**角色回复——解决世界书里写在用户消息侧的 `<act>` `<scene>` `<recall>` 等辅助块被带进判断提示词的问题。
- **参考段数可配**：设置卡（判断AI档）新增「判断时参考几段角色回复」——只看最新 1 段（默认）/ 最近 2 段 / 最近 3 段 / 最近 6 段 / 自定义；选 2 段以上会同时带上更早的角色回复做参考。
- 提取 / 排除规则语义不变：仍在发送前逐段过滤这些角色回复（用户消息本就不发送，不再参与过滤），解析 `<结论>` 前也会对判断AI输出再过滤一次。
- 排障提示：世界书条目里阶段标题的 `##` 后必须有一个空格（`## 间章` 而不是 `##间章`），否则这一段不会被识别为新阶段，内容会被并入上一阶段一起发送。
- 单测 83 → **85/85**（新增：默认只发最新 1 段角色回复、参考 2 段集成、参考段数归一化）。

### v2.14（2026-09-21）

- **提取 / 排除规则语义对齐数据库（修复）**：v2.13 把规则实现成「过滤判断AI的输出」，与数据库填表规则的真实语义不符——数据库是在**发送前**逐条过滤最近剧情里的**角色消息**（用户消息原样发送）。v2.14 对齐：配了 `<content>` 提取规则后，发给判断AI的最近剧情只含各条角色回复里 `<content>` 包着的部分，状态栏、思维链、闲聊不再发送（修复「选了 `<content>` 提示词还是发送其他部分」的问题）；规则在解析结论前也会对判断AI输出再过滤一次（无命中时原样保留，v2.13 行为兼容）。
- **规则测试器**：「提取 / 排除规则」卡底部新增测试区——粘贴任意样例文本，或一键填入**最近一次判断AI的真实输出**（只存内存），用当前草稿规则试跑，直接看到过滤结果、是否命中 `<结论>` 标签、解析出的 YES / NO。
- **常用规则一键添加**：「＋ 排除思维链 `<think>`」「＋ 只留 `<结论>` 段」两个快捷按钮（自动去重）。
- **运行日志页增强**：头部等级统计（信息 / 警告 / 错误 / 调试各多少条）、新增按标签筛选（标签列表自动收集）、新增「导出」.txt 文件；判断AI的原始输出与过滤生效情况记入调试日志（开「采集调试日志」可见）。
- 单测 79 → **83/83**（新增：发送前逐条过滤角色消息集成、思维链发送前削除、留痕字段、规则预览字段）。

### v2.13（2026-09-21）

- **输出提取 / 排除规则**（数据库填表规则同款，复刻 AcuRulePairList 与边界语义）：「判断AI提示词」页新增「输出过滤」卡——提取规则只保留「开始边界~结束边界」之间的内容（含边界、取最后一处命中、多条空行拼接、都没命中不过滤），排除规则删掉「开始边界~结束边界」区间（含边界、支持嵌套、重叠自动合并）；边界匹配不区分大小写，先提取后排除，在解析 `<结论>` 之前执行，留空 = 不过滤。常用来削掉思维链（如排除规则 `<think>` → `</think>`）。规则随提示词草稿一起保存，导入导出 JSON 也会带上；旧导出文件没有规则字段时保留现有规则。
- **运行日志页**（仿数据库 log-buffer）：目录抽屉新增「运行日志」——判断AI检查与结论（含耗时）、阶段推进、绑定变更、拉取模型、切换聊天等事件实时记录（信息 / 警告 / 错误三级 + 可选调试采集），支持等级筛选、复制、清空；内存环形缓冲上限 500 条，不写变量、不上传，刷新页面才清空。
- **命名统一**：界面、提示与默认提示词中的「裁判 / 后台裁判」统一改称「判断AI」，「自动推进」第三档同步更名。
- **内部模块化**：新增两个零依赖内部模块——运行日志模块（仿数据库 `shared/log-buffer.ts`）与边界规则模块（复刻数据库 `utils.ts` 的提取/排除语义），均挂在测试钩子上直接单测（单测 69 → 79）。

### v2.12（2026-09-21）

- **裁判检查频率选择**（数据库填表同款「每 N 层」频率制）：设置卡新增「多久检查一次」——每层检查 / 每 2 层 / 每 3 层 / 每 5 层 / 自定义层数；每条 AI 回复（正文）算一层，正文一到就自动触发，够 N 层才静默问一次裁判，问过重新计数；首次检查立即执行，非法值回退每层。
- **目录导航改名「仪表盘」**：与数据库的页面结构对齐——仪表盘 / API / 划分阶段。

### v2.11.2（2026-09-21）

- **裁判提示词迁移到独立二级页面**：管理页设置卡里点「裁判提示词…」进入，和数据库的提示词抽屉一样——编辑只改草稿，点「保存」才写入；未改动时「放弃修改」「保存」不可点。
- **一键导入 / 导出**：导出把当前草稿存成 JSON 文件（`动态指导助手-裁判提示词.json`），导入支持段数组或 `{segments: [...]}` 包装格式，非法角色自动归 user，导入后需点「保存」生效。
- **恢复默认提示词**：一键载入内置默认段（填表格式三段），同样点「保存」生效。
- **取消「最终提示词注入」**：判断规则并入默认上下文段末尾（与数据库填表上下文段的收尾指令同款写法）；v2.11.1 存过的 judgeFinalPrompt 字段读取时自动清除。
- 旧版 judgePrompt 单模板继续生效，在本页保存一次自动转成段结构。

### v2.11.1（2026-09-21）

- **裁判提示词改为「提示词段」结构**（复刻数据库剧情推进页）：每段可选 system / user / assistant 角色，支持上移/下移/删除、首尾插入，段列表后再加一条「最终提示词注入」（最后一条 user 消息）；占位符 `{{stage}} {{prompt}} {{condition}} {{history}}` 在每段和最终注入里都可用，另附「恢复默认提示词」。
- **默认裁判提示词换成数据库填表格式，面向低智力模型**：system 段给出标签化输出契约（`<依据>` + `<结论>`），assistant 预确认段先替模型答应规则，user 段用【】分区给上下文，最终注入带三条短规则收尾；结论优先读 `<结论>` 标签，模型没按格式输出时回退「回答开头是 YES」的旧规则。旧版自定义提问模板（judgePrompt）继续生效，在设置里改任意一段会自动转成段结构。
- **最大回复长度 / 温度默认值对齐数据库**：60000 / 1，新建预设不再留空；缺省或非法值保存时也回退到这两个默认。
- **模型列表加箭头标注**：拉到模型后下拉框上方出现跳动的「⬇ 模型拉到了，点下面的下拉框选一个」，选中自动填进模型名。
- 细节：酒馆主 API 通道不再叠加 chat_history（最近剧情已通过 {{history}} 写进提示词，避免弱模型被重复内容干扰）。

### v2.11（2026-09-21）

- **API 页整体复刻数据库（shujuku）新版 ApiConfigPanel**：预设选择行（下拉 + ＋新建 + ✕删除图标按钮）、草稿/快照脏检查（「放弃修改」「保存并选中预设 / 保存当前预设」未改动时不可点）、连接方式改为三段开关（酒馆主 API / 自定义 / 酒馆预设）、字段说明文字与数据库一致。
- **补齐数据库 API 页的全部可设参数**：自定义连接新增「附加主体参数」（custom_include_body，YAML）、「排除主体参数」（custom_exclude_body）、「提示词后处理」（八档，含「未选择」原样透传）、「附加请求标头」（每行一个 Header: Value）；「最大回复长度 / 温度」改两列布局。
- **接口全部改走酒馆**（与数据库同一套通道）：「加载模型」改为 POST 酒馆后端 `/api/backends/chat-completions/status`，由酒馆服务器代发（行为同酒馆「测试连接」，无浏览器跨域问题）；按钮在自定义连接下始终可点，直接用当前表单里的端点与密钥，**新建预设不用先保存**。修复 v2.10 填了 URL 和密钥仍点不了/拉了没反应的问题。
- **裁判调用按连接方式分流**：酒馆主 API 走酒馆助手 `generateRaw`；酒馆预设走酒馆连接管理器 `ConnectionManagerRequestService.sendRequest`（不再用 generateRaw 的 proxy_preset）；自定义改为直连酒馆后端 `/api/backends/chat-completions/generate`，请求体复刻数据库的构建方式（协议四选映射：Claude/Gemini 走酒馆原生协议源并自动补 /v1 或剥版本段，OpenAI Responses 回退兼容 OpenAI），四个高级参数真实生效。
- **酒馆预设改为下拉选择**：选项实时读自酒馆连接管理器的 profiles，附「刷新列表」按钮；不再手填预设名。
- **目录抽屉美化**：复刻数据库 Sidebar——品牌区（方块标 + 名称 + 版本副标）、组标题、整宽导航项、当前页主题色高亮、滑入动画（此前抽屉只有骨架、完全没有样式）。
- 命名统一：界面与提示中的「裁判 API 预设」统一改称「API 预设」。
- **迁移注意**：v2.9 及以前存过「酒馆代理预设」的老预设，连接方式会迁移为「酒馆预设」，但代理预设名与连接管理器 profile id 不一定对得上，请在 API 页重新从下拉里选一次。

### v2.10.1（2026-09-21）

- **纯再发布，功能与 v2.10 完全一致**：jsDelivr 对 `@v2.10` 标签别名的边缘与源站缓存持续返回 v2.10 第一版旧字节（209089 B，缺「加载模型按钮启用态」修复），purge 成功后源站仍按旧缓存解析标签，无法原地刷新；改用新标签 `v2.10.1` 换取全新缓存键，在线版 import 指向 `@v2.10.1/index.js`。
- 脚本本体无任何代码改动，仅版本号（`VERSION`、头注释、`meta.json`）；离线版用户不受 CDN 影响，无需重新导入。

### v2.10（2026-09-20）

- **目录式导航**：左上角新增目录按钮（☰），打开抽屉式页面导航，可在「管理」「API」「划分阶段」之间切换，仿 shujuku 新版一级页结构。
- **独立 API 页**：API 预设管理从管理首页迁移到独立页面，按 shujuku 新版 ApiConfigPanel 重做：预设下拉（含新建/删除）、连接方式三段切换（酒馆当前 API / 自定义 / 酒馆连接预设）、自定义时显示接口协议四选（兼容 OpenAI / OpenAI Responses / Claude Messages / Gemini Interactions）、端点 URL、API 密钥、模型名、加载模型按钮与模型列表下拉、最大输出 tokens、温度。
- **字段精简**：删除分类、备注、酒馆代理预设名字段，不再保留兼容 UI；旧版 v1 预设自动迁移（`type: proxy → tavern`、`type: current → main`）。
- **密钥边界不变**：完整预设只存当前浏览器 `localStorage`，API Key 不进入角色变量/聊天变量、不随角色卡导出；`localStorage` 是本机明文存储，共享设备请勿保存敏感 Key。
- **修复**：「加载模型」按钮的启用状态现在跟随连接方式实时更新——此前新建预设（默认「酒馆当前 API」）时按钮被禁用，切到「自定义」后也不会恢复，导致无法拉取模型。

### v2.9（2026-09-20）

- **裁判 API 预设完全独立**：不再依赖 SP·数据库 III。核查 shujuku 最新源码后确认，其公开 `getApiPresets/loadApiPreset/saveApiPreset` 已全部弃用并拒绝外部读取/写入；这就是 v2.8 真机无法拉取预设的根因。v2.9 删除数据库运行依赖，后台裁判统一走酒馆助手 `generateRaw`。
- **仿新版预设管理 UI**：新增独立「裁判 API 预设」分类管理区，预设按分类展示，支持添加/更新、编辑、设为当前和删除；类型包括「酒馆当前 API」「酒馆代理预设」「自定义 API」。代理预设名单从酒馆助手 `getProxyPresetNames()` 读取。
- **保存完整 API 配置**：自定义预设可保存 API 地址、Key、模型、API source、最大输出 tokens、温度与备注；裁判调用时映射到 `generateRaw.custom_api`。选择不存在的本机预设时不发请求、不推进，并明确提示重新选择。
- **密钥边界**：完整预设只存当前浏览器同源 `localStorage`，不写角色变量/聊天变量，不随角色卡导出；角色配置只保存当前预设名。注意 `localStorage` 仍是本机明文存储，共享设备请勿保存敏感 Key。

### v2.8（2026-09-20）

- **裁判可指定数据库 API 预设**：「自动推进」设置卡新增「裁判 API 预设」下拉（选项实时取自 SP·数据库 III 的 `getApiPresets()`）。选定后每次裁判调用前先 `loadApiPreset(预设名)` 把预设应用为数据库当前配置再走 `callAI`——裁判可以用和剧情生成不同的模型/连接（比如在数据库里配一个便宜快的小模型专门做完成判定）。留空 = 跟随数据库当前配置（v2.7 行为）；选了不存在的预设会明确报错、不推进、不静默回落。

### v2.7（2026-09-20）

- **裁判提示词可自定义**：后台裁判的提问不再是固定文案，设置卡里直接改；支持 `{{stage}}`（阶段名）、`{{prompt}}`（阶段正文）、`{{condition}}`（完成条件）、`{{history}}`（最近 6 条剧情，自动剥离隐藏标记）占位符，留空用默认模板。
- **裁判引擎可选**：新增「裁判引擎」设置（存 `config.settings.judgeEngine`）：自动（优先用 SP·数据库 III / shujuku 暴露的 `window.AutoCardUpdaterAPI.callAI` 复用数据库插件的 API 配置，未安装时回落酒馆助手 `generateRaw`）、强制 callAI、强制 generateRaw。诊断页 judge 档会同时列出两个引擎的可用性。
- **完成标记彻底不显示**：发布页新增配套 Regex 资产「动态指导助手 · 隐藏完成标记」（只作用于 AI 输出的显示层，不改聊天文件）。装上后标记从出现的那一刻起就不可见；脚本侧仍会在推进后把标记从存储正文擦掉，聊天历史保持干净。

### v2.6（2026-09-20）

- **新增全局「自动推进」三档设置**（管理页设置卡，存 `config.settings.autoAdvance`）：手动推进（默认，等同 v2.5）、标记判断（没写完成条件的阶段，镜像末尾附通用判断指令和隐藏标记，正文 AI 自己判断时机，零额外请求）、后台裁判（每条 AI 回复后用 `generateRaw` 静默问一次当前阶段是否完成，YES 才推进）。裁判带四重防误判：同绑定并发去重、同一消息最多推进一次、YES 后重新核对进度与最新消息 id、接口缺失或调用失败只提示一次不重试。
- **阶段级 `完成：自动`**：单独给某个阶段开启 AI 自己判断时机，跨档位生效；解析只认整词「自动/自动判断/auto」，「自动门」这类文本不会误判；选区划分往返保留该写法。
- 界面配套：管理页新增「自动推进」设置卡（切换档位后立即重同步镜像）；绑定卡片预览按条件 / 自动判断 / 当前档位分别显示进入下一段的方式；阶段编辑弹层回填并保留「自动」；诊断新增当前档位行，judge 档额外检查 `generateRaw` 接口是否可用。
- 修复：添加 / 移出绑定时会丢失「自动推进」设置（配置重组没带上 `settings`）。

### v2.5（2026-09-20）

- **指导改走「镜像条目」，真正回到原位置**（修复 v2.4 注入跑到提示词最顶上的问题）：扩展提示词的锚点（预设之前 / 主提示词块 / 聊天深度）复刻不了世界书条目的位置——`BEFORE_PROMPT` 会排到预设开头之前，`IN_PROMPT` 会并进主提示词块，所以注入永远不可能落在原来的世界书条目之间。现在不再注入提示词，而是在同一本世界书里维护一个「（动态指导）」镜像条目：克隆原条目的位置、顺序、关键词等全部设置，只把内容换成当前阶段，AI 就在原条目原来的位置（比如某个条目后面）看到它。原条目保持关闭、原文不动；点“移出”删掉镜像、重新打开原条目。
- **不再依赖注入时机**：镜像条目常驻世界书，阶段变化时才改写内容（幂等同步，没变化不写），不再需要每次生成都重新注入，「注入被外部清掉就消失」这一类问题从根上消失。swipe / 重新生成推进过的回复时，镜像会临时换回推进前的阶段。
- **提示词里不再出现插件头部**：发出去的内容就是大纲正文本身，去掉了「[动态指导助手：当前有效内容]」和「以下是作者为当前进度准备的内部创作指导……」这两行。
- **诊断重做**：逐项检查环境、接口、事件后，直接显示每条绑定的位置解析和镜像条目状态（是否存在、多少字、是否与当前阶段一致），一眼分清问题在哪；修复「复制诊断报告」在 iframe 里因 `Document is not focused` 失败的问题（改走 textarea 兜底，两种都失败时如实提示）。
- 升级自动处理：≤2.4 的提示词注入残留（助手通道和原生通道）在第一次启动时按旧 id 自动清理。

### v2.4（2026-09-20）

- **注入改双通道，老版本酒馆助手也能注入**：聊天内注入优先走酒馆助手 `injectPrompts`；当这个接口缺失（老版本助手没有它——这正是一直看不到注入的最可能原因）或调用报错时，自动退回酒馆原生 `setExtensionPrompt`（聊天内深度注入，深度和角色跟随条目），这是几乎任何版本酒馆都有的老接口。两条通道共用一个注入 id，清理时两边都清。
- **管理页新增「诊断注入链路」**：逐项体检脚本实例、酒馆助手版本、11 个接口、3 个事件、原生扩展提示、每条绑定的条目与阶段状态，并对两条注入通道各做一次试注；结果一键复制，排查问题时把报告发给作者即可。

### v2.3.1（2026-09-20）

- **修复注入消失的严重 bug**（v2.0–v2.3 一直存在）：旧版注入逻辑做了“内容没变就跳过”的去重，而酒馆助手会在脚本重载等时机自动清掉持久注入（4.8.17 起），一旦被清，去重逻辑会让指导永远补不回来，提示词查看器里也看不到。现在恢复 1.3.6 的注入纪律：每次生成事件都“先撤掉旧注入、再重新注入”，并且注入带 `once: true` 只对下一次请求生效——任何外部清理都会在下一轮生成时被自动补回。
- dryRun 预组装（提示词查看器查看提示词时触发）现在同样会注入当前指导，查看器里能直接确认注入内容。

### v2.3（2026-09-20）

- 标题卡新增 **↑↓ 整块搬移**：阶段、附加、常驻、备注都能在“看分段”里上移/下移一个位置（标题、标签、正文一起动，前言不动）。阶段顺序即进度顺序；附加的“从：/到：”按阶段名引用，换位后自动跟随。
- **常驻的前后位置现在有注入语义**：常驻块写在所有阶段之前时，注入里单独成节排在当前阶段内容之前（`## 常驻提示`）；写在阶段之间或最后则保持在“同时有效的附加内容”区。常驻标题卡的副标题会写明当前位置；选区划分的重建保留这个位置，常驻 chip 设置区也可以直接切换。

### v2.2（2026-09-20）

- “划分阶段”编辑器新增第三个视图“选区划分”：正文铺成一段连续文字，鼠标拖选或触屏“点选头尾”（点一下设开头、再点一下设结尾）把文字收进“已准备”，点“分配给「当前分段」”完成归属。每个分段一种底色，点已上色的文字可切换当前分段、移除选中段；`+ 阶段` / `+ 附加` 直接新建，名称、完成条件、附加起止阶段在 chip 下方的设置区修改。
- 选区划分是为 v2 正文标题体系重做的实现，不另外存数据：分配、移除、删除分段会立刻按归属把正文重建成 `## 标题` 写法（并入块自动吸收进目标阶段，没分配的文字收在最前面不发送）；改名、改条件不触发重建，切视图或保存时一次性写回，打字不丢焦点。载入与重建往返幂等，拖选捕获带触屏滚动保护。
- 编辑器工具栏统一成“看分段 / 选区划分 / 编辑原文”三视图切换；头部“未保存”提示现在包含选区模式里尚未重建的修改。

### v2.1（2026-09-20）

- 支持多条绑定：可以把好几个条目同时添加为指导条目。每个条目添加后保持关闭，只有当前阶段的切片注入回它原来的位置（按深度插入跟随深度与角色，角色定义前/后走原生锚点），其余内容暂时不被 AI 看到；一条回复里的完成标记会按各自的阶段分别推进。每条注入使用带绑定指纹的独立 ID，互不覆盖。
- 每条绑定在管理页显示为一张卡片：条目名与来源世界书、第 N 段 · 共 M 段、上一段 / 下一段、折叠的“现在发给 AI 的内容”，以及“重新划分 / 回到第一段 / 移出”一排不常用操作。点“移出”会重新打开条目并删掉它在当前聊天的进度；条目被意外打开时卡片会警告并给出“立即关闭条目”。
- 管理页简化成“卡片列表 + 一个添加区”：两个下拉选世界书和条目，旁边就是“划分阶段”和“添加为指导条目”；空绑定时显示三步上手引导。编辑器里“保存并绑定”更名“保存并添加”；已添加的条目在编辑器里只提供“保存”，修改正文后进度按阶段名自动对上，不会被误归零。
- 2.0 的单个绑定配置和扁平聊天进度读取时自动迁移到多绑定结构并写回，无需手动操作。
- 措辞统一为“关闭 / 打开条目”“添加 / 移出”，与管理页实际行为一致。

### v2.0.3（2026-09-20）

- 来源条目使用“角色定义前 / 角色定义后”位置时，指导现在通过酒馆原生扩展提示锚点注入回对应位置，不再一律落到聊天末尾；管理页“现在发给 AI 的内容”会写明实际位置。“按深度插入”仍跟随条目的深度与角色；示例消息、作者注释等没有注入通道的位置保持放在聊天末尾。
- 修复切换聊天后注入可能缺失的问题：旧版本在新聊天里如果阶段内容和上一个聊天相同，会因为缓存键未清理而跳过重新注入，导致请求里没有当前阶段。现在切换聊天时同时清空缓存键，保证每个聊天都重新注入。
- 拿不到酒馆原生扩展提示接口的环境会退化为聊天末尾注入，并给出一次性提示。

### v2.0.2（2026-09-19）

- “划分阶段”新增“编辑原文”视图：直接改世界书正文的文本框，增删文字、调整顺序、自己写 `## 阶段名` 都可以，切回“看分段”会重新解析。保存不再因为“没有点击过段落”而禁用，正文可以在编辑器里一次写完。
- 点段落添加标题时可以指定“从这一段的哪一行开始”，长段落不必先切开就能在中间起一个新阶段。
- 新增 `合并到：阶段名`：一个阶段可以吃掉几段不连续的文字，进度仍然只算目标阶段一次。编辑器里对应“并入已有阶段”类型，改名时引用同步更新。
- 指导注入位置跟随来源条目：条目使用“按深度插入”时按相同深度与角色注入，管理页会写明实际注入位置；条目被重新启用时仍然在生成前再次禁用。
- 修复手机端魔法棒入口点击后管理页不出现的问题：面板改为显式写死视口高度，不再依赖在部分手机浏览器里会算成 0 高度的定位方式；入口恢复酒馆自带的魔法棒条目结构，主题样式不会走样。

### v2.0.1（2026-09-18）

- 修复手机端魔法棒入口点击后管理页没有出现的问题：入口与全屏面板会挂到真正包含魔法棒菜单的主页面，并兼容嵌套 iframe、旧 Android 触摸事件和跨域顶层壳层。
- 修复生成时当前阶段提示词消失的问题：注入内容改为跟随进度即时同步，页面打开、绑定、上一段 / 下一段、完成标记推进和切换聊天后都会立刻写入当前阶段；生成事件仍会等待世界书读取、旧注入清理和注入完成后再继续组装请求，两条路径合起来保证请求里一定有当前阶段的指导。
- 手机端管理页点击后直接铺满当前视口，不再显示成右下角浮动按钮或居中小窗口；脚本重复加载时会清理旧入口、旧面板和旧监听器。
- 保留 v2.0 的正文标题格式与段落点击编辑器；不恢复 1.x 的字符选区、正文 Base64 标记和多处 layout 备份。
- 新增生成等待、进度变化即时注入、swipe 回退阶段、嵌套 iframe、跨域顶层窗口与手机全屏入口回归测试。

### v2.0（2026-09-18）

- 阶段结构改为直接写在世界书正文中：用 `## 阶段名` 分段、用 `完成：` 设置推进条件，不再依赖字符选区、隐藏布局标记或多处布局备份。
- 重做“划分阶段”编辑器：点击段落添加标题，点击标题改名、设置条件、改类型或删除；支持首行转标题和按空行自动分段，手机不再需要拖选文字。
- 新增明确的 `[附加]`、`从：`、`到：` 阶段范围格式，起止阶段都包含在内；支持 `[常驻]` 与不会发送的 `[备注]`。
- 保留旧文本模板解析，并为 1.x 隐藏选区划分提供一次性正文转换：按阶段重组已分配文字，未分配文字保留为备注，保存后读回校验。旧选区转换前暂停注入，迁移时自动转义原文中的标题和规则标签，防止结构误识别。
- 支持用行首反斜杠把标题或规则标签保留为普通正文，发送时去掉转义符。
- 管理页集中展示当前阶段、实际指导内容和解析提醒；保留聊天级独立进度、上一段、下一段与回到第一段，重新绑定从第一段开始。
- 生成前校验并禁用来源条目，使用一次性动态注入；自动完成后重新生成当前回复时，尝试使用完成前的阶段。
- 文档同步新版创作流程、格式边界和迁移方法；项目版本为 v2.0，在线加载固定至 Git 标签 v2.0.0。

### v1.3.6（2026-09-18）

- 撤回 v1.3.5 的“新聊天继承角色进度”：进度重新只记在当前聊天里，新建聊天从第一段开始，“重置到第一段”只影响当前聊天。
- 阶段划分改为同时写进世界书条目的隐藏扩展数据和正文末尾的隐藏标记，并在角色变量、全局变量里留备份。跟着角色卡分享出去的条目，别人导入后不需要重新划分；本机扩展数据丢失时会自动从正文标记或备份恢复并写回。
- 标注器、管理页预览和发送给 AI 的内容都会自动去掉这段隐藏标记，不会泄露到对话里。
- 保存划分后会立刻读回校验，写不进世界书时明确提示“已保存在角色备份里”，不再出现“保存好的阶段消失”的假象。
- 修复手机上待分配高亮在存在系统选区时被一直压暗、看起来像“选好的段落和美化都没了”的问题：现在只在手指拖选过程中变淡，松手立刻恢复。
- 修复点选头尾时手指滑动会让系统选区一路扩到全文开头的问题：点选模式下一开始滑动就收起系统选区，同时保留手势开始前已经放下的开头和已准备的段落。
- 修复滑动收起系统选区、还原之前的范围后，继续选取会让还原的范围再次消失的问题；滑动刚结束的短暂保护期内产生的选区会推迟复查，不再被直接丢弃。
- 点选模式的两条位置标记加上“开头”“结尾”文字标签，实心和虚线分别对应起点和终点，手机上更容易分辨。
- 管理页新增“阶段划分”一行，显示划分保存在世界书条目里、还是刚从角色备份恢复。

### v1.3.5（2026-09-18）

- 剧情进度改为同时记录在角色上：新建聊天会自动从上次的剧情阶段继续，不再回到第一段重来。
- 每个聊天仍保留自己的进度，切换回旧聊天不会因为新聊天的推进而被打乱。
- 管理页的进度区新增说明文字，明确写出这个角色记住的进度、是否刚刚继承，以及“重置到第一段”会影响之后新建的聊天。
- 重置弹窗同步说明影响范围：当前聊天和之后新建的聊天都会从第一段重新开始。
- 重新绑定指导条目时会把角色级进度一起归零，保持与旧版本一致的行为。

### v1.3.4（2026-09-18）

- 把“什么时候进入下一阶段”从折叠设置中移出来，始终显示在剧情阶段列表下方，并标明当前正在设置哪个阶段。
- 折叠项只保留阶段名称、标记颜色、排序和删除，减少普通用户找不到关键设置的困惑。
- 修复选择文字后待分配高亮被系统选区临时盖住的问题；现在高亮会保持可见，系统选区存在时只会稍微变淡。
- 点选头尾完成一段后主动收起系统选区，让自绘高亮立即恢复。
- 补充编辑器内部 `hidden` 元素样式，避免已经隐藏的面板在部分主题里仍然露出。

### v1.3.3（2026-09-18）

- 点选头尾支持连续累积多个不连续范围：选完第一段后第一段会保留，可以继续点选第二段、第三段，最后一起分配。
- 增加明确的步骤提示条，显示“下一步：点这一段的开头”“已放下开头，下一步：点这一段的结尾”“已准备 N 段，可以继续点选或直接分配”。
- 修复手机端滑动收起原生选区后，之前还原的范围在后续选择时被覆盖消失的问题；拖选模式也会保留已经准备好的多个范围。
- “清除所选标记”会同时清除待分配范围；存在待分配内容时，按钮会显示当前段数。

### v1.3.2（2026-09-18）

- 新增“点选头尾”选择方式，手机上默认启用：点一下这一段的开头，再点一下结尾，中间的文字会自动选中，不再依赖系统的文字选择手柄。
- 修复手机上选好一段文字后往下滑动，选区被浏览器一路扩展成“把上面的内容全选”的问题；现在滑动会立刻收起原生选区，并保留手势开始前选好的范围。
- 点选模式下会显示开头、结尾两条位置标记，以及还没分配的虚线预览，确认无误后再点“分配给当前阶段”。
- 保留原来的“拖选文字”方式，两种方式可以随时切换；切换时清空当前选择。
- 手机端仍在同一屏内完成划分与保存，页面没有横向溢出。

### v1.3.1（2026-09-18）

- 修复手机端“划分提示词阶段”标注器被裁切、正文摸不到也拖不动的问题。
- 手机端标注器改为纵向滚动布局，阶段列表可以横向滚动，底部“保存划分 / 保存并绑定”始终可见。
- 增加 `selectionchange` 与 `touchcancel` 选区捕获，拖动系统选择手柄后“分配”按钮能正常启用。
- 触屏端阶段设置默认折叠为“阶段 N 设置”，点击展开，新建阶段时自动展开。
- 提示词正文显式启用文字选择，修复移动端无法长按拖选的问题。

### v1.3（2026-09-18）

- 新增“划分提示词阶段”标注器：在管理页选中条目后打开，直接拖选提示词原文上色。
- 支持在一个世界书条目里自由划分任意数量的剧情阶段，同一阶段可以包含多处不连续文字。
- 支持“常驻提示”，这部分内容会在所有阶段一起发送。
- 每个阶段可以设置名称、标记颜色，以及“什么时候进入下一阶段”的自动推进条件。
- 阶段划分保存在条目的隐藏扩展数据中，世界书正文保持原样，跟随角色卡或世界书一起分享。
- 选区使用原文片段与前后文定位，作者修改大纲后自动重新定位；整段被删除时会给出提醒。
- 保留旧版 `【内容：名称】` 模板解析，旧作者卡不需要改动。

### v1.2（2026-09-18）

- 移除页面下方的五个酒馆助手按钮，改为左下角魔法棒菜单中的单一入口。
- 升级脚本时自动清理 v1.1 遗留的五个按钮；重新导入 v1.2 JSON 也会同步清空按钮配置。
- 增加独立管理页，在一处完成世界书与条目选择、当前状态查看、上一段、下一段和重置。
- 修复已绑定世界书仍被识别为“没有绑定”的问题，增加 TavernHelper、角色卡原生字段与附加世界书设置三层识别。
- 同时兼容新版条目数组、旧版 `entries` 数组和原生条目字典，修复世界书有条目却无法列出的情况。
- 在管理页显示识别来源和具体错误，方便区分角色绑定、全局世界书与群聊场景。

### v1.1（2026-09-18）

- 增加脚本按钮的运行时自动注册。
- 支持只用一行远程 `import` 加载完整功能。
- 为 GitHub 与 jsDelivr 在线发布补充使用说明。

### v1.0（2026-09-18）

- 建立单世界书条目、多内容块的核心解析格式。
- 实现角色级绑定配置和聊天级独立进度。
- 实现生成前只注入当前主线与当前附加内容。
- 实现隐藏完成标记自动推进，以及下一段、上一段、重置按钮。
- 实现来源条目自动禁用和绑定失效后的重新查找。
